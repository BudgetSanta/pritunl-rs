use crate::{
    socket::{self, Response},
    Client,
};

impl Client {
    //pub fn put_token(&self) -> Response {
    //    socket::put(self, "/token")
    //}

    //pub fn delete_token(&self) -> Response {
    //    socket::delete(self, "/token")
    //}
}
